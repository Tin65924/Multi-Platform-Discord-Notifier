Drop your hardcoded embed image here as cover.jpg, then set PUBLIC_BASE_URL in .env to this app's public URL.
Discord fetches the image, so localhost will NOT work - the app must be publicly reachable or use a public image URL instead (dashboard Webhook tab or EMBED_IMAGE_URL in .env).
